/**
 * PNG OG 图片生成脚本
 * 使用 Canvas API 在 Node.js 中生成 OG 图片
 * 
 * 运行方式：在浏览器中打开 public/og-image-generator.html
 * 点击下载按钮获取 og-image.png
 * 将文件放入 public/ 目录
 * 
 * 注意：SVG 格式的 og-image.svg 作为 fallback 仍然保留
 * 部署后建议运行一次生成器以获得 PNG 格式图片
 * 微信等平台不支持 SVG 预览，PNG 格式兼容性更好
 */
