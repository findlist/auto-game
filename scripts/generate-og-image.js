/**
 * OG 图片 PNG 生成脚本
 * 用法：在浏览器中打开 public/og-image-generator.html，会自动下载 PNG 文件
 * 将下载的 og-image.png 放入 public/ 目录即可
 * 
 * 此脚本不依赖任何 npm 包，纯 Canvas API 生成
 */
// 这个文件仅作为文档说明，实际生成在 og-image-generator.html 中完成
