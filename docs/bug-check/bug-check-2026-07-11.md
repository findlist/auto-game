# Bug 检查与修复报告 - auto-game

## 检查时间
2026-07-11 01:25

## 检查范围
- **build 结果**：`tsc -b && vite build` 通过，61 modules transformed，无类型错误
- **代码审查范围**：
  - `src/game/` 下的游戏逻辑代码（levelGenerator、solver、dailyChallenge、dailyLeaderboard、hintItems、levelEditor、replayShare、replayVideo、settings、themeManager、soundEngine、visitTracker）
  - `src/components/GameBoard.tsx`（定时器清理、回放状态、ref 赋值）
  - `src/App.tsx`（限时模式通关处理）
  - `src/pages/SettingsPage.tsx`、`src/pages/LevelEditorPage.tsx`
  - `public/sw.js`（Service Worker 缓存）
- **git diff HEAD~1 分析**：基于上次提交 `7679d1c` 以来的所有变更

## 已修复并提交的问题

提交哈希：`e492f8c`（11 个纯修复文件，已推送至 origin/main）

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| S1 | P0 | public/sw.js | CACHE_VERSION 为 `v1.15.0`，与 package.json 版本 `1.14.0` 不一致，导致缓存键错乱 | 改为 `v1.14.0` | e492f8c |
| S2 | P0 | public/sw.js | `cache.addAll` 是原子操作，任一资源 404 会导致全部预缓存失败 | 改为 `Promise.allSettled(STATIC_ASSETS.map(url => cache.add(url)))` 逐个缓存 | e492f8c |
| S9(每日挑战) | P0 | src/game/dailyChallenge.ts | 不可解关卡兜底用最后一次 shuffle（可能仍不可解）覆盖 resultTubes | 移除 `resultTubes = shuffledRetry` 覆盖，保留初始 finalTubes | e492f8c |
| S10 | P1 | src/game/dailyLeaderboard.ts | 排行榜按时间戳截断，同一日期多条记录时丢失历史最佳成绩 | 改为按日期分组保留每日最佳（星级优先，步数次之），再按日期保留最近 MAX_ENTRIES 天 | e492f8c |
| S11 | P1 | src/game/hintItems.ts | 每日提示道具领取非幂等：先 addHintItems 后 setItem，setItem 失败时可重复领取 | 调整顺序：先 setItem 标记后 addHintItems 发放 | e492f8c |
| S12 | P0 | src/game/levelEditor.ts | 颜色整除校验 `t.capacity === count` 仅匹配单管容量，颜色层数为容量整数倍时误判无效 | 改为 `count % t.capacity === 0` | e492f8c |
| S13 | P1 | src/game/replayVideo.ts | 视频录制无 `onerror` 处理，录制出错时 Promise 永远挂起，UI 卡死 | 添加 `recorder.onerror = () => reject(...)`，Promise 改为 `(resolve, reject)` 签名 | e492f8c |
| S14 | P0 | src/game/replayShare.ts | 空步数回放正则 `D(.+)$` 要求至少 1 字符，0 步通关无法解码 | 改为 `D(.*)$` 允许 0 字符 | e492f8c |
| S16 | P1 | src/game/solver.ts | BFS 队列存储完整路径 `Tube[][]`，但仅使用末尾状态，内存随深度指数增长 | 队列改为只存 `{ tubes: Tube[]; steps: number }` | e492f8c |
| M13 | P1 | src/game/settings.ts | `loadSettings` 返回 `DEFAULT_SETTINGS` 原始引用，外部修改会污染默认值 | 改为 `return { ...DEFAULT_SETTINGS }` | e492f8c |
| M14 | P1 | src/game/themeManager.ts | 无效主题名导致 `THEMES[themeName]` 为 undefined，后续访问抛 TypeError 白屏 | 添加 `\|\| THEMES.classic` 防御 | e492f8c |
| M18 | P1 | src/game/soundEngine.ts | `startBGM` 未恢复 suspended 的 AudioContext，非用户交互场景下 BGM 静默失败 | 添加 `ctx.resume()` 调用 | e492f8c |

## 已修复但未提交的问题（与并行开发变更混合）

以下修复已应用到工作区，但因文件同时包含并行自动开发的功能/UI 变更，无法作为纯 bug 修复提交，留待自动开发提交时一并入库。

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 不提交原因 |
|------|----------|------|----------|----------|------------|
| S3 | P1 | src/game/visitTracker.ts | `recordSessionDuration` 累加会话时长后未重置 `lastSessionStart`，下次保存时重复累加 | 累加后增加 `data.lastSessionStart = Date.now()` | 文件为 auto-dev 新建，混合变更 |
| S4 | P1 | src/components/GameBoard.tsx | 多个 `setTimeout` 未跟踪，组件卸载后回调可能 setState 到已卸载组件 | 添加 `timersRef` 收集所有 timer ID，卸载时统一清理 | 与 auto-dev 功能变更混合 |
| S5 | P1 | src/components/GameBoard.tsx | `replayTimerRef` 在组件卸载时未清理 | cleanup useEffect 中 `clearTimeout(replayTimerRef.current)` | 同上 |
| S6 | P1 | src/components/GameBoard.tsx | 回放按钮文本依赖 `replayTimerRef.current`（ref 修改不触发重渲染），播放状态不更新 | 新增 `isReplayPlaying` state 驱动按钮文本 | 同上 |
| S7 | P0 | src/App.tsx | 限时模式通关后 `setTimedScore(newScore)` 触发 GameBoard 重新生成关卡（因 timedScore 在其 useEffect 依赖中），导致胜利弹窗 500ms 后消失 | 从 `handleWin` 移除 `setTimedScore`，仅更新最高分 | 与 auto-dev UI 变更混合 |
| S8 | P1 | src/components/GameBoard.tsx | `tubesRef.current = tubes` 在渲染体内直接修改 ref，React 18 严格模式下可能读到中间态 | 改为在 `useEffect(() => { tubesRef.current = tubes; }, [tubes])` 中赋值 | 同 S4 |
| S9(关卡生成) | P0 | src/game/levelGenerator.ts | generateLevel/generateEndlessLevel/generateTimedLevel 不可解兜底用最后一次 shuffle 覆盖 | 移除 `solvableTubes = shuffledRetry` 覆盖 | 与 auto-dev 难度曲线变更混合 |
| M15 | P1 | src/pages/LevelEditorPage.tsx | `handleRemoveTube` 中 `forEach((t, i) => t.id = i)` 直接修改 filter 返回的原对象引用，突变 state | 改为 `.map((t, i) => ({ ...t, id: i }))` 创建新对象 | 与 auto-dev CSS 变更混合 |
| M19 | P1 | src/pages/SettingsPage.tsx | 导出存档 `__version` 为 `1.1.0`，与当前应用版本 `1.14.0` 不符 | 改为 `1.14.0` | 与 auto-dev CSS 变更混合 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|------|----------|------|----------|------------|
| S15 | P1 | src/pages/SettingsPage.tsx | 重置进度功能清理不完整，多个 localStorage key 未统一管理，可能遗漏新增 key | 涉及全局 key 清单统一管理，改动范围较大，需评估是否重构 key 管理方式 |

## 记录的轻微问题（P2，未修复）

- **M4**（src/components/GameBoard.tsx）：死局音效"可能重复播放"——经分析 `useEffect` 依赖 `isDeadlock` 布尔值，仅在 false→true 转换时触发 `SoundEngine.deadlock()`，并非重复播放。每次进入死局状态播放一次属预期行为，降级为 P2。

## 验证结果
- 修复后 build：**通过**（`tsc -b && vite build`，61 modules transformed，无错误）
- 已提交的 11 个文件均通过 TypeScript 类型检查和 Vite 构建
- 工作区中的混合文件修复也通过 build 验证（但未单独提交）
