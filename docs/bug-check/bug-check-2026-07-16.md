# Bug 检查与修复报告 - auto-game

## 检查时间
2026-07-16 14:30

## 检查范围
- **build 结果**：`tsc -b && vite build` 通过，67 个模块转换成功，无类型错误
- **代码审查范围**：
  - src/game/ 下 26 个游戏逻辑文件（levelGenerator、solver、adaptiveDifficulty、dailyChallenge、weeklyChallenge、achievements、statsTracker、soundEngine、settings、storageKeys、themeManager、visitTracker、dailyCheckin、dailyLeaderboard、dailyRecommend、playedModes、replayShare、replayVideo、shareImage、announcements、hintItems、weekendBonus、levelEditor、seededRandom、types、dailyRecommend）
  - src/components/ 下 3 个组件（GameBoard、TubeView、ParticleEffect）
  - src/pages/ 下 7 个页面 + App.tsx + main.tsx
  - public/sw.js + manifest.json（PWA）
- **git diff HEAD~1 分析**：最近一次提交（8f118a5）主要是版本号同步至 v1.32.0、FAQ 补充、SEO 更新、清理 scripts 目录，源码逻辑变更集中在 App.tsx（FAQ/changelog 文案）和版本号字符串，无游戏逻辑变更

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| 1 | P0 | src/game/solver.ts:103-107 | solveBFS 达到节点上限时乐观返回 solvable:true，与各生成器兜底逻辑叠加导致不可解关卡被采纳（影响 generateLevel/generateEndlessLevel/generateTimedLevel/generateDailyChallenge/generateWeeklyChallenge） | 改为保守返回 solvable:false，宁可让生成器重试也不输出可能无解的关卡 | 待提交 |
| 2 | P0 | src/game/dailyCheckin.ts:34-52 | loadRecord 未校验 history/rewards 是否为数组，JSON 中为 null 时 push/includes 抛 TypeError 崩溃；totalCheckins 等数字字段为字符串时 +=1 拼接 | loadRecord 中校验 Array.isArray 和 Number.isFinite，非法值回退默认 | 待提交 |
| 3 | P0 | src/game/dailyLeaderboard.ts:21-36 | getDailyLeaderboard 直接断言 JSON.parse 结果为数组，非数组数据 sort 抛 TypeError | 增加 Array.isArray 校验，非数组返回空数组 | 待提交 |
| 4 | P0 | src/game/announcements.ts:134-145 | getReadIds 用 new Set(JSON.parse(data))，若 data 为字符串则按字符拆分导致已读状态完全错乱 | 校验 Array.isArray 后再构造 Set | 待提交 |
| 5 | P0 | src/game/replayShare.ts:18-30 | encodeReplay 未校验 from/to 范围，>=36 时 toString(36) 产生多字符，与解码端固定 2 字符/步假设不符，导致后续步骤错位、回放数据损坏 | 编码前校验 0-35 范围，越界抛错；App.tsx 调用处补充 try-catch 降级 | 待提交 |
| 6 | P0 | src/App.tsx:740-751 | generateReplayUrl 调用未 try-catch，encodeReplay 抛错时整个 handleReplayShare 崩溃 | 包裹 try-catch，失败时仅用文案分享 | 待提交 |
| 7 | P0 | src/game/replayVideo.ts:149-157 | recorder.onstop 正常完成路径未释放 stream tracks（onerror/catch 路径已释放），CanvasCaptureMediaStreamTrack 持续占用 canvas/GPU 资源 | onstop 中补充 stream.getTracks().forEach(t => t.stop()) | 待提交 |
| 8 | P1 | src/App.tsx:323-338, 2366-2369, 2439-2442 | 统计页/百科页在 render 阶段调用 setNewAchievements，违反 React 纯渲染原则，可能触发"Cannot update a component while rendering"警告甚至无限重渲染 | 移至 useEffect，依赖 page 变化触发 | 待提交 |
| 9 | P1 | src/components/GameBoard.tsx:341-369 | handleReset 未重置 showParticles/settledTubes/pouringTo/movesPulse/starRating/showReplay/showShareImage，胜利后点击"再来一局"残留旧关卡动画状态；replayTimerRef 未清理 | 补充状态重置 + 清理 replayTimerRef | 待提交 |
| 10 | P1 | src/components/GameBoard.tsx:449-457 | replayTimerRef 的 setTimeout 在组件卸载时未清理，回放自动播放中切页会触发已卸载组件 setState | 新增卸载 cleanup useEffect | 待提交 |
| 11 | P1 | src/pages/LevelEditorPage.tsx:104-121 | handleExport 的 navigator.clipboard.writeText 未 catch，非 HTTPS 环境产生未捕获 Promise rejection | 补充 .catch 降级 + clipboard 存在性判断 | 待提交 |
| 12 | P1 | src/pages/LevelEditorPage.tsx:405-415 | CustomLevelPlayer.handleReset 未重置 pouringTo/showParticles | 补充重置 | 待提交 |
| 13 | P1 | src/game/levelEditor.ts:97-105 | importLevelCode 未校验 layers.length <= capacity，超容量试管导致 canPour 判断失真 | 导入时截断层数至 capacity | 待提交 |
| 14 | P1 | src/game/replayVideo.ts:66-72 | drawHeader 中 '⭐'.repeat(stars) + '☆'.repeat(3-stars)，stars 越界(>3或<0)时 repeat(负数) 抛 RangeError | clamp stars 到 0-3 | 待提交 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|------|----------|------|----------|------------|
| 1 | P0 | src/game/levelGenerator.ts:104-129 等 | 关卡生成器兜底返回已确认不可解的 finalTubes（5次重试失败时） | 修复 solver.ts 的 P0 根因后，isSolvable 不再误判，生成器会持续重试。但要彻底保证可解需实现反向生成法，属较大改动，需评估 |
| 2 | P0 | src/game/soundEngine.ts:6-13,143 等 | 全局 AudioContext 从不关闭无 destroy 方法；BGM/音效 setTimeout 链无法外部清除 | 需提供 destroyAudioContext() 和 timer Set 管理，涉及全局架构改动，且 SPA 生命周期内 AudioContext 复用是合理设计，需评估是否值得改动 |
| 3 | P1 | src/pages/ColorEncyclopediaPage.tsx 多处 | 多个子游戏(ColorSequenceGame/ColorPerceptionTest/ColorPairMatch/ColorReactionTest/ColorMixer)的 setTimeout 未清理，组件卸载后仍触发 setState | 涉及 10+ 处 setTimeout，统一改造为 cancelled 标志位/timer ref 工作量大，可能引入新问题，需评估 |
| 4 | P1 | src/components/GameBoard.tsx:275-310 | 胜利时多个 setTimeout（归位波纹/星星音效/粒子清除/onWin回调）未保存 timer id | 需用 ref 数组收集并统一清理，涉及胜利流程改动，且 onWin 500ms 延迟是设计意图，需评估 |
| 5 | P1 | src/game/adaptiveDifficulty.ts:150-152 + GameBoard.tsx:112-127 | extraEmptyTubes:-1 应用后未重新验证可解性，可能将可解关卡变为不可解 | solver.ts 修复后影响降低，但 GameBoard 中重新验证需调用 isSolvable（性能开销），且保留至少1个空管已有缓解，需评估 |
| 6 | P1 | src/game/weekendBonus.ts:53-71 + hintItems.ts:26-32 | claimWeekendBonus/claimDailyHintBonus 写标记成功但 addHintItems 内部失败时玩家丢失奖励 | 需两阶段提交或回滚机制，涉及多个函数协调，需评估 |
| 7 | P1 | src/game/announcements.ts:21-58,113,181,243 | getActiveAnnouncements 用 Date.now() 作 createdAt 导致公告永远是"新公告"；dayEpoch 用 UTC 天数与本地日期不一致 | 需为公告定义固定发布时间戳 + 统一日期计算逻辑，涉及数据结构变更，需评估 |
| 8 | P1 | src/game/visitTracker.ts:92-107 | recordSessionDuration 改变 lastSessionStart 语义；返回可变引用 | 需分离 sessionStartTime/lastActiveTime 字段，涉及存储结构变更，需评估 |
| 9 | P1 | src/game/settings.ts:175-188 | _cache 不响应跨标签页 storage 事件，多标签页设置不同步 | 需注册 storage 事件监听器，影响全局缓存策略，需评估 |
| 10 | P1 | src/game/types.ts:48,51-54 | updateColors 不更新 COLOR_KEYS 快照，主题扩展新增颜色键时生成器无法选用 | 需改为 getter 函数，影响所有引用 COLOR_KEYS 的模块，需评估 |
| 11 | P1 | src/game/solver.ts:85 | solveBFS 使用 Array.shift() 导致 O(n²) 性能问题 | 改用头指针索引，虽是性能优化但涉及核心算法改动，需评估 |
| 12 | P1 | public/sw.js:57-59,70-72 | 后台更新缓存时 cache.put 未 catch，可能产生未捕获 rejection | 需补充 .catch，但 SW 逻辑改动需谨慎测试，需评估 |
| 13 | P1 | src/game/replayVideo.ts:152 | URL.createObjectURL 未提供释放机制，多次生成视频 Blob 内存累积 | 需提供 releaseReplayVideo 方法并改造调用方，需评估 |
| 14 | P1 | public/manifest.json:5 | start_url 硬编码线上地址，本地开发/其他域名部署 PWA 启动异常 | 改为相对路径可能影响线上 PWA 标识，需评估 |

## 记录的轻微问题（P2，未修复）
- GameBoard.tsx:169 等 - handleTubeClick 残留 console.log('[debug]') 调试日志
- GameBoard.tsx:61 - shareImageRef 定义但未实际使用（死代码）
- TubeView.tsx:70-71 - onTouchEnd 中 preventDefault 冗余
- TubeView.tsx:158-171 - React.memo 比较函数未比较 onClick（当前父组件传入稳定引用，暂无影响）
- ParticleEffect.tsx:30 - animationRef 初始值 0，建议改为 null
- ParticleEffect.tsx:45 - resize 监听未设 passive
- AchievementsPage.tsx:22 - getAll() 每次渲染调用未缓存
- ColorEncyclopediaPage.tsx:417-420 - render 阶段写入 localStorage
- StatsPage.tsx:14-18 等 - render 阶段大量 localStorage.getItem
- App.tsx:67 - deferredPrompt 使用模块级全局变量
- App.tsx:2203 - onMove 每次移动都写 localStorage（建议节流）
- main.tsx:23 - trackVisit 模块顶层调用，HMR 下重复执行
- main.tsx:11-17 - SW 注册未处理 updatefound/controllerchange
- solver.ts:47,90 - getNextStates 元组第一个元素未使用（内存浪费）
- levelGenerator.ts:383-384 - checkDeadlock 不可达代码
- seededRandom.ts:21-23 - nextInt(min,max) 对 min>max 无防御
- dailyChallenge.ts:105-107 - dayNumber 跨夏令时边界可能偏移
- dailyRecommend.ts:31-34 - starsRecord[lvl] 可能是字符串
- dailyLeaderboard.ts:106 - e.stars===3 严格等于可能漏掉字符串"3"
- achievements.ts:426 - loadState 类型守卫未排除数组
- shareImage.ts:37-45 - Math.random() 导致分享图不确定
- dailyCheckin.ts:144-146 - reset() 缺少 try-catch
- announcements.ts:271-275 - saveDailyQuizResult 去重逻辑允许同一天多次保存

## 验证结果
- 修复后 build：**通过**（tsc -b && vite build，67 模块转换成功，无类型错误）
- 修复前 build：通过（本次检查未发现构建阻断问题，Bug 多为运行时/数据损坏类）
- 修复涉及文件：src/game/solver.ts、src/game/dailyCheckin.ts、src/game/dailyLeaderboard.ts、src/game/announcements.ts、src/game/replayShare.ts、src/game/replayVideo.ts、src/game/levelEditor.ts、src/App.tsx、src/components/GameBoard.tsx、src/pages/LevelEditorPage.tsx（共 10 个文件）
