# Bug 检查与修复报告 - auto-game

## 检查时间
2026-07-13 14:50

## 检查范围
- **build 结果**：`tsc -b && vite build` 通过，67 modules transformed，无类型错误
- **代码审查范围**：
  - `src/game/` 全部游戏逻辑代码（weeklyChallenge、levelGenerator、dailyChallenge、achievements、statsTracker、soundEngine、themeManager、settings、visitTracker、weekendBonus、playedModes、dailyRecommend、storageKeys 等）
  - `src/components/` 组件代码（GameBoard、TubeView、ParticleEffect）
  - `src/pages/` 页面代码（ColorEncyclopediaPage、LevelEditorPage、StatsPage、SettingsPage 等）
  - `src/App.tsx` 主应用状态管理
  - `public/sw.js`、`public/manifest.json`、`src/main.tsx` PWA 配置与入口
- **git diff HEAD~1 分析**：最新提交 `3b5a09a`（fix: 修复周挑战不可解关卡兜底逻辑和 level=-4 显示问题）仅含 2 个文件。工作区有大量未提交的 auto-dev 功能变更（周末奖励、色彩百科、游玩模式记录等）与本轮 bug 修复混合。

## 已修复但未提交的问题（与 auto-dev 功能变更深度混合）

以下 8 个修复已应用到工作区并通过 build 验证，但因工作区中 auto-dev 未提交变更与修复代码深度混合，修复文件依赖多个未跟踪文件（storageKeys、dailyRecommend、playedModes、ColorEncyclopediaPage 等），无法单独提交修复而不破坏构建完整性。

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 |
|------|----------|------|----------|----------|
| B1 | P0 | src/App.tsx | `handleWin` 中 `saveBestScore(currentLevel, winMoves)` 和 `progress.completedLevels.push(currentLevel)` 无条件执行，对每日挑战(-1)/无尽(-2)/限时(-3)/周挑战(-4)模式会写入 `bestScores[-1/-2/-3/-4]` 并将负数加入 `completedLevels`，污染最佳成绩和通关进度数据 | 仅在 `currentLevel > 0`（普通模式）时更新 progress 和 bestScores，保留 `newCompleted` 变量供后续成就检查使用 |
| B2 | P0 | src/App.tsx | 自动存档恢复逻辑仅处理 'endless' 和 'timed' 模式，daily/weekly 模式走 else 分支只设置 `currentLevel` 不设置模式标志。恢复后 `isDailyMode`/`isWeeklyMode` 为 false，导致 `handleWin` 中 `saveStars(-1/-4)` 误执行、每日/周挑战记录不保存、普通模式成就误触发 | 添加 `daily` 和 `weekly` 分支，恢复时设置对应的模式标志（`setIsDailyMode(true)` / `setIsWeeklyMode(true)`），同时更新恢复对话框文案 |
| B3 | P1 | src/components/GameBoard.tsx | 关卡变化 useEffect 重置了 tubes、moves、isWon 等状态，但未重置 `timeLeft` 和 `isTimeUp`。限时模式通关后进入下一关时，`timeLeft` 保留上一关剩余时间，`isTimeUp` 若为 true 则倒计时 useEffect 提前 return，限时模式无法正常继续 | 在 useEffect 中添加 `setTimeLeft(timedDuration)` 和 `setIsTimeUp(false)`，依赖列表添加 `timedDuration` |
| B4 | P1 | src/components/GameBoard.tsx | `handleReset` 未重置 `timeLeft` 和 `isTimeUp`。限时模式时间到后点击"重置"，`isTimeUp` 仍为 true，倒计时不会重启，游戏卡在"时间到"状态 | 在 `handleReset` 中添加 `setTimeLeft(timedDuration)` 和 `setIsTimeUp(false)`，依赖列表添加 `timedDuration` |
| B5 | P1 | src/game/visitTracker.ts | `recordSessionDuration` 直接 `JSON.parse(raw)` 操作数据，未与 `DEFAULT_DATA` 合并。若 localStorage 中旧数据缺少 `totalSessionDuration` 字段，`undefined + duration` 得到 `NaN`，被持久化后导致统计页面展示 NaN，无法自愈 | 改用 `loadVisitData()` 加载数据（自带默认值合并），并在累加时添加 `\|\| 0` 兜底 |
| B6 | P1 | src/game/settings.ts | `GameSettings.get()` 直接返回 `this._cache` 引用而非副本。外部代码修改返回的 Settings 对象会污染缓存但不写入 localStorage，导致"设置改了但没保存"的幽灵 bug，页面刷新后设置回退 | `get()` 返回 `{ ...this._cache }` 副本，外部修改不影响缓存 |
| B7 | P1 | src/game/weekendBonus.ts | `claimWeekendBonus` 中 `localStorage.setItem` 失败时 catch 静默忽略，程序继续执行 `addHintItems(1)` 发放道具。但领取记录未持久化，刷新后 `hasClaimedWeekendBonus()` 返回 false，玩家可重复领取 | setItem 失败时 `return null`，不发放道具，避免重复领取 |
| B8 | P1 | src/game/weeklyChallenge.ts | `saveWeeklyRecord` 中 `totalCompletions` 每次调用都 +1，不受幂等保护。若同一周重复调用（如 handleWin 被触发两次），`totalCompletions` 虚高。而 `WEEKLY_RECORD_KEY` 和连胜逻辑都有幂等保护，唯独 `totalCompletions` 没有 | 仅在 `lastWeekSeed !== seed`（本周首次完成）时累加 `totalCompletions` |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|------|----------|------|----------|------------|
| B9 | P1 | src/components/GameBoard.tsx | `handleTubeClick` 中多个 `setTimeout`（倾倒动画 300ms、胜利归位 idx*80ms、星星音效、粒子效果 3000ms、onWin 500ms）均未保存返回值，组件卸载后仍会触发 setState，造成内存泄漏 | 修复需引入 `timersRef` 收集所有定时器 ID 并在卸载时统一清理，涉及 handleTubeClick 内多处改动，范围较大可能引入新问题 |
| B10 | P1 | src/components/GameBoard.tsx | `replayTimerRef` 用于回放自动播放定时器，回放弹窗关闭时清理，但组件卸载时无统一 cleanup | 同 B9，需新增 useEffect cleanup，且回放逻辑复杂需单独评估 |
| B11 | P1 | src/pages/LevelEditorPage.tsx | `CustomLevelPlayer` 的 `handleTubeClick` 中 `setTimeout`（倾倒动画、粒子效果、onWin）未保存返回值，卸载时未清理 | 同 B9，需引入 timersRef 管理机制 |
| B12 | P1 | src/game/statsTracker.ts | `recordWin` 无 `isWeekly` 参数，周挑战（level=-4）的三个模式标志均为 false，导致 `recordWin` 将 -4 当作普通关卡：写入 `levelStats[-4]`、`recentRecords` 中 mode 记录为 'normal' | 修复需修改 `recordWin` 签名添加 `isWeekly` 参数并同步修改 `GameRecord['mode']` 类型，跨多文件改动，需评估是否重构统计接口（与昨日 W8 相同） |
| B13 | P0 | public/sw.js | fetch 事件对所有请求（含 HTML 导航请求）统一使用 cache-first 策略。新版本发布后，SW activate 清理旧缓存，但 install 阶段未预缓存 Vite hashed 资源（/assets/index-[hash].js），用户离线刷新时 cache miss → 网络失败 → 白屏 | 修复需对导航请求改用 network-first 策略并补充 hashed 资源预缓存，涉及 SW 核心逻辑改动，可能影响离线体验，需单独评估和测试 |
| B14 | P1 | public/sw.js | `self.skipWaiting()` 和 `self.clients.claim()` 在 `event.waitUntil` 之外执行，可能在预缓存完成前就跳过 waiting 状态 | 修复需将 `skipWaiting()` 移入 `waitUntil` Promise 链，改动较小但影响 SW 生命周期，建议与 B13 一起评估 |
| B15 | P1 | public/sw.js | 后台更新缓存时（fetch fallback 分支）未校验 `response.type`，可能缓存 opaque CORS 响应，占用配额且后续 `caches.match` 返回不可用响应 | 需补充 `response.type === 'basic'` 校验，建议与 B13 一起评估 |
| B16 | P1 | public/manifest.json | `start_url` 使用绝对 URL `https://game.niuzi.asia/`，开发环境无法安装 PWA | 改为相对路径 `/?source=pwa`，但可能影响生产环境统计，需评估 |
| B17 | P1 | src/App.tsx | `page === 'stats'` 和 `page === 'encyclopedia'` 渲染分支中直接调用 `setNewAchievements`，在渲染期间 setState 不符合 React 规范。虽然成就检查是幂等的（unlock 后立即持久化，不会无限循环），但仍可能触发 React 警告 | 修复需将检查移入 `useEffect`，但涉及渲染逻辑重构，且不影响功能，优先级较低 |

## 记录的轻微问题（P2，未修复）

- **B18**（src/game/levelGenerator.ts）：`checkDeadlock` 在所有试管为空时（胜利状态）返回 true（误判为死局）。逻辑上不正确，但正常流程中 `checkWin` 先于 `checkDeadlock` 调用，所有试管为空时已判定胜利，不会进入死局检查
- **B19**（src/components/GameBoard.tsx）：胜利弹窗"下一关"按钮在周挑战模式下文本为"➡️ 下一关"，实际行为是返回首页，文本不够准确
- **B20**（src/game/achievements.ts）：`checkLevelAchievements` 中 `state` 和 `newState` 两次 `loadState()` 之间存在时间差，单线程同步假设下无实际风险
- **B21**（package.json）：版本号 `1.15.0` 与 `sw.js` 的 `v1.21.0`、`App.tsx` 的 `1.21.0` 严重不一致，若未来通过 CI 自动更新版本号可能导致 SW 缓存版本不更新
- **B22**（src/App.tsx）：`setupPWAInstallPrompt` 全局事件监听器无清理函数，当前在模块级调用一次不会泄漏，但 HMR 环境下可能重复注册
- **B23**（public/sw.js）：`offline.html` 离线回退 `caches.match('/offline.html')` 可能返回 undefined，`respondWith` 收到 undefined 会抛 TypeError。但 `Promise.allSettled` 容错放过了预缓存失败，实际触发概率极低

## 验证结果
- 修复后 build：**通过**（`tsc -b && vite build`，67 modules transformed，无错误）
- 8 个 P0/P1 修复均通过 TypeScript 类型检查和 Vite 构建
- 所有修复仅改动最小必要代码，未引入新功能、未重构
- 修复未提交：工作区中 auto-dev 未提交变更与修复代码深度混合，修复文件依赖多个未跟踪文件（如 App.tsx 依赖 dailyRecommend、playedModes、ColorEncyclopediaPage 等），无法单独提交修复而不破坏构建完整性。待 auto-dev 提交时一并入库
