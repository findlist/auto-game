# Bug 检查与修复报告 - auto-game

## 检查时间
2026-07-18 00:00

## 检查范围
- build 结果：`tsc -b && vite build` 构建通过，无类型错误
- 代码审查范围：src/game/ 全部模块、src/components/ 全部组件、src/pages/ 全部页面、src/App.tsx
- git diff HEAD~1：最近一次提交（v1.36.0）新增色彩百科搜索高亮、累计游玩天数成就、日历热力图等功能

## 已修复的问题
| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P1 | src/pages/ColorEncyclopediaPage.tsx | `highlightText` 函数在 `parts.map()` 中使用带 `g` 标志的 `regex.test(part)`，由于 `g` 标志正则的 `lastIndex` 在多次调用间有状态，导致交替的匹配项返回 `false`，部分搜索关键词未被高亮 | 改为使用不带 `g` 标志的新正则 `testRegex` 做精确匹配测试，避免 `lastIndex` 状态污染 | fc7c005 |

## 未修复的问题（需评估）
| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 无 | - | - | - | - |

## 记录的轻微问题（P2，未修复）
1. **src/game/levelGenerator.ts** — `generateLevel`、`generateEndlessLevel`、`generateTimedLevel` 三个函数中关卡生成逻辑大量重复（颜色池创建、洗牌、分配试管、可解性验证），可抽取公共函数。但重构范围大，不属于 bug 修复范畴。
2. **src/game/announcements.ts** — `DAILY_COLOR_QUIZ` 数组中所有题目 `difficulty` 均为 `'easy'`，但类型定义包含 `'medium'` 和 `'hard'`。题库难度分布单一，不影响功能。
3. **src/game/achievements.ts** — `ACHIEVEMENT_DEFS` 中 `total_100_moves` 成就描述为"累计操作达到 1000 步"，但 id 和触发条件用的是 1000，id 名称 `total_100_moves` 有误导性（id 历史遗留）。
4. **src/components/GameBoard.tsx** — `handleTubeClick` 中 `setTimeout(() => setPouringTo(null), 300)` 和 `setTimeout(() => setMovesPulse(false), 300)` 等定时器未在组件卸载时清理，但这些是极短延时（300ms）且仅做 UI 状态重置，实际影响可忽略。
5. **src/App.tsx** — `handleExportReplayVideo` 中 `generateReplayVideo` 异步操作完成后调用 `setReplayVideoUrl` 等 setState，如果用户在生成期间关闭弹窗（组件卸载）可能产生 React 警告，但该场景概率极低且仅影响开发模式日志。

## 验证结果
- 修复后重新运行 `npm run build`（tsc -b && vite build）：✅ 构建通过
- TypeScript 类型检查：✅ 无错误
- Vite 生产构建：✅ 70 模块转换成功，1.29s 完成
- 修复提交已推送到 main 分支：fc7c005
