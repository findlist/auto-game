# Bug 检查与修复报告 - auto-game

## 检查时间
2026-07-14

## 检查范围
- build 结果：`npm run build`（tsc -b && vite build）通过，67 modules transformed，built in 2.11s
- 代码审查范围：
  - `src/game/` 下全部模块：levelGenerator、solver、types、adaptiveDifficulty、weeklyChallenge、dailyChallenge、soundEngine、settings、statsTracker、visitTracker、storageKeys、themeManager、achievements、announcements、dailyCheckin、dailyLeaderboard、dailyRecommend、hintItems、levelEditor、playedModes、seededRandom、weekendBonus、replayShare、replayVideo、shareImage
  - `src/components/` 下全部组件：GameBoard、TubeView、ParticleEffect
  - `src/pages/` 下全部页面：AboutPage、AchievementsPage、PrivacyPage、SettingsPage、StatsPage、LevelEditorPage、ColorEncyclopediaPage
  - `src/App.tsx`、`src/main.tsx`
  - `public/sw.js`、`public/manifest.json`
- git diff HEAD~1 变更分析：重点关注游戏逻辑错误、状态管理、类型错误、内存泄漏、移动端兼容、PWA 问题

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| 1 | P0 | src/game/replayVideo.ts:170-192 | playNext 中 pour 抛错（move.from/to 越界）会导致递归 setTimeout 链中断，recorder.stop() 永不执行，Promise 永久挂起，UI 卡死 | 用 try/catch 包裹 pour 调用，出错时停止录制、释放 stream、reject Promise | 2458b4a |
| 2 | P1 | src/game/replayVideo.ts:156-158 | recorder.onerror 触发时仅 reject Promise，未调用 recorder.stop()、未停止 stream.getTracks()，导致 MediaRecorder 和 Canvas captureStream 资源泄漏 | onerror 中补充 recorder.stop() 和 stream.getTracks().forEach(t => t.stop()) | 2458b4a |
| 3 | P1 | src/game/replayVideo.ts:208-211 | generateReplayAsImages 中 pour 抛错未捕获，函数抛错转为 reject，调用方未 catch 会变成未处理 rejection | 添加 try/catch 兜底，出错时保留初始状态渲染 | 2458b4a |
| 4 | P1 | src/game/hintItems.ts:15 | getHintItems 中 `parseInt(data, 10) || 0` 在数据损坏（如 "abc"）时返回 0 而非初始值 3，导致新用户或数据损坏用户丢失初始道具 | 改为 `const n = parseInt(data, 10); return isNaN(n) ? 3 : n` | 未提交¹ |
| 5 | P1 | src/game/weekendBonus.ts:30 | loadRecord 中 `JSON.parse(data)` 返回 null 时不抛错，后续 `record.lastClaimedDate` 抛 TypeError 导致调用栈崩溃 | 校验 `parsed && typeof parsed === 'object'`，非对象回退默认值 | 未提交¹ |
| 6 | P1 | src/game/achievements.ts:392 | loadState 中 `JSON.parse(data)` 返回 null/数字/字符串时不抛错，后续 `state.unlocked` 抛 TypeError | 校验 `parsed.unlocked` 为对象，否则回退默认值 | 未提交¹ |
| 7 | P1 | src/game/announcements.ts:228 | getDailyQuizHistory 中 `JSON.parse(data)` 返回非数组时，后续 `.find()`/`.push()` 抛 TypeError | 校验 `Array.isArray(parsed)`，非数组返回 `[]` | 未提交¹ |
| 8 | P1 | src/game/playedModes.ts:15 | getPlayedModes 中 `JSON.parse(data)` 返回字符串时，`.includes()` 检查子字符串行为错误，`.push()` 抛 TypeError | 校验 `Array.isArray(parsed)`，非数组返回 `[]` | 未提交¹ |
| 9 | P1 | src/game/levelEditor.ts:55 | getCustomLevels 中 `JSON.parse(data)` 返回 null/对象时，`.findIndex()`/`.push()` 抛 TypeError | 校验 `Array.isArray(parsed)`，非数组返回 `[]` | 未提交¹ |
| 10 | P1 | src/game/levelEditor.ts:88-93 | importLevelCode 未校验 compact 结构和字段类型，损坏的分享码可导入 capacity 为 NaN/负数的无解关卡 | 校验 `Array.isArray(compact.t)`、capacity 为正整数、layers 为数组 | 未提交¹ |
| 11 | P1 | src/game/dailyCheckin.ts:86 | checkin 中 `record.currentStreak += 1`，若 currentStreak 因数据损坏为 undefined/字符串，得到 NaN 或字符串拼接 | 前置校验 `Number.isFinite(record.currentStreak)`，否则重置为 0 | 未提交¹ |
| 12 | P1 | src/game/dailyLeaderboard.ts:101-102 | getDailyStats 中 reduce/Math.min 未校验数字，e.moves 为 undefined 时产生 NaN 污染 bestMoves/avgMoves | 过滤 `Number.isFinite(m)`，空数组时 bestMoves 返回 null | 未提交¹ |
| 13 | P1 | src/pages/SettingsPage.tsx:53-69 | 重置进度硬编码 9 个键，遗漏 autosave/endless/daily-leaderboard/recent/visits 等，重置后刷新会从 autosave 恢复旧进度 | 改用 `RESETTABLE_KEYS.forEach(k => localStorage.removeItem(k))` | 未提交¹ |
| 14 | P1 | src/pages/SettingsPage.tsx:71-93 | 导出存档硬编码 13 个键，遗漏 recent/autosave/daily-leaderboard/custom-levels 等，导出→重置→导入丢失数据 | 改用 `BACKUP_KEYS.forEach(k => ...)` | 未提交¹ |
| 15 | P1 | src/pages/SettingsPage.tsx:104-108 | 导入存档未校验 data[k] 类型，非字符串值被 toString() 写入 "[object Object]"，后续 JSON.parse 崩溃 | 增加 `typeof data[k] === 'string'` 校验 | 未提交¹ |
| 16 | P1 | src/pages/ColorEncyclopediaPage.tsx:769,790,799 | ColorReactionTest 中三个 setTimeout 返回值未存入 timerRef，卸载时无法清理，回调在卸载后执行 onComplete 触发非预期成就弹窗 | 三个 setTimeout 返回值均存入 timerRef，cleanup 统一清理 | 未提交¹ |

> ¹ 未提交原因：工作区有大量并行自动开发的未提交修改（30+ 文件），这些文件中并行开发的修改与本任务的修复混合在同一文件中无法安全分离。提交这些文件会同时推送并行开发的内容，可能干扰其工作流。仅 replayVideo.ts 在修改前是 clean 状态，已单独提交。其余修复已在本地通过 build 验证，待并行开发完成后统一提交。

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|------|----------|------|----------|------------|
| 1 | P1 | src/game/weekendBonus.ts:43-61 | claimWeekendBonus 写入非原子：setItem(WEEKEND_BONUS_KEY) 成功后 addHintItems(1) 可能失败，导致标记已领但道具未加 | 修复涉及回滚逻辑（失败时需回退 WEEKEND_BONUS_KEY），复杂度高，可能引入新问题 |
| 2 | P1 | src/game/hintItems.ts:44-60 | claimDailyHintBonus 非原子写入：先写标记后发道具，setItem 失败时道具未加 | 原代码注释明确说明"先写入标记保证幂等性"，改为先发道具后写标记会引入重复领取风险，需权衡 |
| 3 | P1 | src/game/announcements.ts:236-249 | saveDailyQuizResult 使用硬编码键名 'daily_quiz_history'，未纳入 STORAGE_KEYS 统一管理，重置/导出不覆盖 | 修复需在 storageKeys.ts 中新增键并加入 RESETTABLE_KEYS/BACKUP_KEYS，同时改动 announcements.ts，影响面较大 |
| 4 | P1 | src/pages/ColorEncyclopediaPage.tsx:364-397 | ColorPairMatch 在 setMatches 状态更新器函数内执行大量副作用（嵌套 setState、SoundEngine、localStorage、onComplete），StrictMode 双重触发导致音效播放两次、成就重复触发 | 修复需将副作用移出 updater 并重构状态管理逻辑，改动范围大 |
| 5 | P1 | src/pages/ColorEncyclopediaPage.tsx:323-336 | ColorPairMatch 在 setTimeLeft 状态更新器函数内执行副作用（SoundEngine.timeUp/tick、setGameState），StrictMode 双重触发 | 同上，修复需重构为 useEffect 监听 timeLeft 变化 |
| 6 | P1 | src/pages/SettingsPage.tsx + ColorEncyclopediaPage.tsx | 色彩百科小游戏的高分/进度键（color_test_best、color_pair_best_*、color_sequence_best、color_reaction_*、color_mixer_count）完全未纳入重置和导出 | 修复需在 storageKeys.ts 中新增 10+ 个键并纳入 RESETTABLE_KEYS/BACKUP_KEYS，同时改动 SettingsPage 和 ColorEncyclopediaPage，影响面大 |
| 7 | P1 | src/game/replayShare.ts:18 | encodeReplay 未校验 from/to 范围，>=36 时 toString(36) 返回多字符导致解码错位 | 当前游戏试管数不会超过 35，实际不触发；修复需更改编码格式，影响向后兼容性 |

## 记录的轻微问题（P2，未修复）

- src/components/GameBoard.tsx:169,171,195,199,212 等 - 7 处 `console.log('[debug] ...')` 调试日志残留
- src/components/TubeView.tsx:105,109 - 2 处 `console.log('[debug] ...')` 调试日志残留
- src/game/dailyRecommend.ts:5-6 - import 语句重复（`import { getDailySeed } from './seededRandom'` 和 `import { SeededRandom } from './seededRandom'` 应合并）
- src/game/announcements.ts:252-258 - getReadIds 未验证 JSON.parse 返回值是数组，字符串会被 Set 迭代为字符
- src/game/announcements.ts:172-176,216-220 - getTodayColorKnowledge/getTodayColorQuiz 用 UTC dayEpoch，与 getTodayTip 用本地日期不一致，跨日时显示不同步
- src/game/dailyLeaderboard.ts:63-83 - addDailyLeaderboardEntry 截断逻辑不一致（仅超 MAX_ENTRIES 时去重）
- src/game/dailyLeaderboard.ts:47-51 vs 74 - getBestForDate 只比较 moves，addDailyLeaderboardEntry 去重时 stars 优先，"最佳"定义不一致
- src/game/shareImage.ts:37-45 - 装饰圆点用 Math.random，同一战绩生成两次图片不完全一致
- src/game/shareImage.ts:77-80 - data.stars 未校验范围（>3 或 <0 时显示异常）
- src/game/seededRandom.ts:21-23 - nextInt 未校验 min <= max
- src/game/achievements.ts:453-463 - checkLevelAchievements 中 state vs newState 引用混用，可读性差
- src/pages/SettingsPage.tsx:18-51 - toggle 系列函数使用过期 settings 闭包，快速连续切换可能状态不一致
- src/pages/SettingsPage.tsx:53-69 - 重置后未通知父组件刷新，timedHighScore 等状态显示旧值直到刷新
- src/pages/ColorEncyclopediaPage.tsx:614-622 - ColorSequence 通关第 15 关时 showResult=true，显示"游戏结束"而非"完美通关"
- src/pages/ColorEncyclopediaPage.tsx:776-777 - avgTime 实际存的是最后一轮反应时间，非平均值，与 UI 显示"上次平均反应"不符
- src/pages/ColorEncyclopediaPage.tsx:135-146 等 - 常量数组（CLOSE_COLORS、DIFFICULTY_CONFIG 等）定义在组件内部，每次渲染重新创建
- src/pages/StatsPage.tsx:571-578 - clipboard.writeText 的 Promise 未加 .catch
- src/pages/StatsPage.tsx:14 - StatsTracker.get() 在每次渲染时调用，未使用 useMemo
- src/pages/AchievementsPage.tsx:20 - AchievementManager.getAll() 在每次渲染时调用
- src/game/dailyCheckin.ts:141-143 - reset 未用 try/catch 包裹 localStorage.removeItem
- src/game/dailyLeaderboard.ts:116-118 - clearDailyLeaderboard 未用 try/catch
- src/game/replayShare.ts:57 - generateReplayUrl 对 IPv6 地址解析错误（`host.split(':')[0]` 返回 '['）
- src/game/levelEditor.ts:152-154 - generateLevelId 碰撞概率较高（Math.random() * 1000）

## 验证结果
- 修复后 build：**通过**（tsc -b && vite build，67 modules transformed，built in 2.11s）
- 已提交修复（replayVideo.ts）：通过 build 验证，已推送至 origin/main（commit 2458b4a）
- 未提交修复（其余 13 个文件）：已在本地通过 build 验证，因与并行开发修改混合未提交
