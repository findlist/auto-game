# UI 优化记录 2026-07-29 (game)

## 问题证据

### 1. SettingsPage 设置开关可点击 div 缺乏键盘可访问性

- **文件**：`src/pages/SettingsPage.tsx`
- **问题**：4 处 `<div className="setting-item" onClick={...}>`（音效、震动、背景音乐、重置进度）均使用 div + onClick，未添加 `role="button"`、`tabIndex={0}`、`onKeyDown`，键盘用户和屏幕阅读器用户无法操作。
- **对照**：`src/components/TubeView.tsx` 第 113-122 行的同类可点击元素已完整添加键盘属性，属项目内视觉契约不一致。
- **评分**：V=4, S=4, C=5, A=5, R=1, E=2 → Score=41

### 2. LevelEditorPage 编辑器试管缺键盘属性 + "撒销"错别字

- **文件**：`src/pages/LevelEditorPage.tsx`
- **问题 A**：第 243 行编辑器试管 `<div className="tube" onClick={...} onContextMenu={...}>` 无 `role="button"`、`tabIndex={0}`、`onKeyDown`，键盘用户无法编辑关卡。
- **问题 B**：第 485、493 行按钮文案"撒销"为错别字，项目其他文件（GameBoard.tsx、GameOverlays.tsx）均为"撤销"。
- **评分**：V=3, S=3, C=5, A=4, R=1, E=2 → Score=34

## 修改文件

1. `src/pages/SettingsPage.tsx`
2. `src/pages/LevelEditorPage.tsx`

## 修改内容

### SettingsPage.tsx

- 新增模块级 `handleEnterPress` 辅助函数，让 div[role=button] 响应 Enter/Space
- 4 处 setting-item div 添加 `role="button"`、`tabIndex={0}`、`onKeyDown`
- 3 处 toggle 开关添加 `aria-pressed` 属性（音效/震动/背景音乐）
- 重置进度项添加 `role="button"`、`tabIndex={0}`、`onKeyDown`（高风险操作，无 aria-pressed）

### LevelEditorPage.tsx

- 编辑器试管 div 添加 `role="button"`、`tabIndex={0}`、`aria-label`、`onKeyDown`（Enter/Space 触发 handleTubeClick）
- 第 485、493 行"撒销"统一改为"撤销"

## 验证结果

- `npm run build`（tsc -b && vite build）：✅ 通过，exit code 0
- 118 模块转换成功，无 TypeScript 错误
- 构建产物正常输出

## 评分变化

| 文件 | 修改前 | 修改后 |
|---|---|---|
| SettingsPage.tsx | 未评估 | 8（良好，主要可访问性问题已修复） |
| LevelEditorPage.tsx | 未评估 | 8（良好，键盘属性 + 错别字已修复） |

## 后续候选

- `src/components/SmartRecommendSection.tsx`：第 138 行推荐卡片缺键盘属性（Score=28），同文件其他 3 张卡片已正确实现，下次优先处理
- `src/components/HomeChrome.tsx`：第 43 行 Unicode 组合字符可能渲染异常，需进一步确认
- `src/components/LevelSelectSection.tsx`：第 131-144 行搜索输入框缺 aria-label
