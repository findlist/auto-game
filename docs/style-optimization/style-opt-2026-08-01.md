# UI 优化记录 2026-08-01 (game)

## 修改文件

### src/components/HomeTopSection.tsx — recent-play-card 键盘可访问性

**问题**：L126 `recent-play-card` 是可点击 div（onClick 根据模式触发 onDailyChallenge/onEndlessMode/onTimedMode/onSelectLevel），但缺 `role="button"` / `tabIndex` / `onKeyDown`，键盘用户无法触发。同文件 L150 `replay-last-card`、L204 `weekly-challenge-banner`、L221 `weekend-bonus-banner` 均已有完整键盘可访问性，此为遗漏项。

**修改**：
- L126：添加 `role="button" tabIndex={0} onKeyDown`，onKeyDown 复用 onClick 同一模式分支逻辑（与同文件 L150/L204/L221 内联模式一致）

**评分**：Score=40, C=5, R=1（通过门槛 Score>=22, C>=4, R<=1）

### src/components/HomeFooterSection.tsx — custom-level-card 键盘可访问性

**问题**：L59 `custom-level-card` 是可点击 div（onClick 调用 onPlayCustomLevel(lv)），但缺 `role="button"` / `tabIndex` / `onKeyDown`，键盘用户无法触发。同文件 L88 `collapse-header`、L101 `saved-recipes-entry`、L112 `today-achievement-card` 均已有完整键盘可访问性（使用同文件已定义的 handleEnterPress helper），此为遗漏项。

**修改**：
- L59：添加 `role="button" tabIndex={0} onKeyDown={handleEnterPress(() => onPlayCustomLevel(lv))}`，复用同文件 L29 已定义的 handleEnterPress helper

**评分**：Score=40, C=5, R=1（通过门槛 Score>=22, C>=4, R<=1）

## 关联问题

两个文件均为「首页可点击卡片缺键盘可访问性」同一关联问题，分属 HomeTopSection（快速重玩）与 HomeFooterSection（自定关卡）。

## 验证

- `tsc -b`：通过（exit 0，类型检查零错误）
- `npm run build`：vite build 因本地 Node v14 不支持 `??=`（ES2021）无法完成，tsc 类型检查通过；服务器 Node >=18 环境不受影响（与 2026-07-31 同一环境限制）

## 下次方向

- HomeTopSection.tsx 已无遗漏的可点击 div（recent-play-card 修复后，所有可点击卡片均完整）
- ColorEncyclopediaPage.tsx L1382 `mixer-milestone-toast`（可点击关闭）可后续处理
- 其他 components 目录文件可继续巡检
