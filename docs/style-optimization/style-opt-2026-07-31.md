# UI 优化记录 2026-07-31 (game)

## 修改文件

### src/pages/AchievementsPage.tsx — 可点击卡片键盘可访问性

**问题**：L210 `ach-hint-card ach-hint-recent` 是可点击 div（onClick 切换时间线视图），但缺少 `role="button"` / `tabIndex` / `onKeyDown`，键盘用户无法触发。同文件视图切换（L247/L253）、分类筛选（L302）、稀有度筛选（L318）均为 `<button>` 已天然可访问；项目内 SettingsPage、LevelEditorPage、SmartRecommendSection、TubeView、HomeFooterSection 的可点击 div 均已添加键盘可访问性，此为遗漏项。

**修改**：
- 新增模块级 `handleEnterPress` helper（与 SettingsPage 一致）
- L210：添加 `role="button" tabIndex={0} onKeyDown={handleEnterPress(() => setViewMode('timeline'))}`

**评分**：Score=28, C=5, R=1（通过门槛 Score>=22, C>=4, R<=1）

## 验证
- `npm run build`（项目根目录）：通过（exit 0，tsc -b + vite build 均成功）

### src/pages/ColorEncyclopediaPage.tsx — 色彩专题卡片键盘可访问性

**问题**：L1687 `seasonal-topic-banner` 和 L1730 `color-topic-card` 是可点击 div（onClick 展开颜色详情），但缺 `role="button"` / `tabIndex` / `onKeyDown`，键盘用户无法触发。同文件 L1775 `color-encyclopedia-card` 已有完整键盘可访问性模式，此为遗漏项。

**修改**：
- 新增模块级 `handleEnterPress` helper（与 AchievementsPage/SettingsPage 一致）
- L1687 `seasonal-topic-banner`：添加 `role="button" tabIndex={0} onKeyDown`
- L1730 `color-topic-card`：添加 `role="button" tabIndex={0} onKeyDown`

**评分**：Score=36, C=5, R=1（通过门槛 Score>=22, C>=4, R<=1）

## 验证
- `npx tsc -b`：通过（exit 0）
- `npm run build`：vite build 因本地 Node v14 不支持 `??=`（ES2021）无法完成，tsc 类型检查通过；服务器 Node >=18 环境不受影响

## 下次方向
- HomeTopSection.tsx L126 `recent-play-card`、HomeFooterSection.tsx L59 `custom-level-card` 仍为可点击 div 缺键盘可访问性，可后续处理
- ColorEncyclopediaPage.tsx L1382 `mixer-milestone-toast`（可点击关闭）也可后续处理
