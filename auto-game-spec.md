# auto-game 自主进化 Agent 规范

> 版本：v2.0
> 更新日期：2026-07-28
> 阶段：已上线产品的数据驱动维护期

## 成功标准

Agent 只为可验证的玩家价值、稳定性、性能或安全性工作。提交数、版本号、CSS 动效数量、组件拆分数量和文档轮次不是产出指标。没有合格任务时零修改、零提交结束属于成功。

禁止为了保持活跃而继续拆 Hook、抽常量、增加装饰动画、更新版本号或补写迭代流水账。

## 有效信号

- 线上错误、崩溃和可复现玩法缺陷。
- CI、测试、构建、PWA 或缓存一致性失败。
- Core Web Vitals、首屏体积、内存或帧率出现量化退化。
- 玩家留存、关卡完成率、失败率、功能使用率等产品指标。
- 玩家反馈或明确批准的新关卡、玩法和运营任务。
- 有证据支持的安全、隐私或可访问性问题。

没有玩家数据或故障证据支持的“体验可能更好”不得自动实施。

## 选择门槛

候选按 0-5 分评估：玩家价值 V、严重度 S、证据 C、紧迫度 U、风险 R、成本 E。

`Score = 3V + 3S + 3C + U - 2R - E`

自动实施必须满足：`Score >= 24`、`C >= 4`、`R <= 2`，并有明确验收标准。单次最多实施最高分的 1 项；无合格项立即停止。

## 权限边界

可自动实施：可复现的局部缺陷、失败测试和构建修复、明确的性能回归修复、已授权任务的低风险步骤。

必须授权：新玩法和关卡、数值平衡、存档结构、埋点和隐私变化、新依赖、大范围 UI 改版、PWA 缓存策略或公共架构重构。

默认拒绝：纯重命名、常量抽取、无指标支持的 Hook/组件拆分、纯装饰动画、仅用于发布新版本号的改动。

## 运行与验证

1. 默认每天评估一次，只读取上次评估后的增量信号。
2. 最多列出 5 个候选并评分。
3. 无合格任务时不得修改文件或创建报告。
4. 实施前写明玩家影响和验收标准。
5. 运行与改动风险相称的测试；玩法、存档、PWA 或构建链变化时执行全量验证。
6. 验证失败只处理本次问题，不切换无关替代任务。

## Git 与记忆

- 达到评分门槛、`C >= 4`、`R <= 1`、工作区干净、相关测试与构建全部通过，且不涉及授权范围时，必须自动按单任务 commit 并执行 `git push origin HEAD`。
- 自动推送前必须再次执行 `git status --short` 和 `git diff --cached --name-only`，确认暂存区只包含本任务文件；只能精确 `git add <本任务文件>`，禁止 `git add -A`。
- `R = 2` 的任务可以实施和验证，但不得自动提交或推送，必须报告结果等待用户决定。
- 单次运行最多创建并推送 1 个提交；push 失败时保留当前提交并报告，不得追加新提交或反复重试。
- 满足上述自动推送门槛时允许由 Git 集成触发既有站点部署；主动发布版本、修改部署配置或创建发布产物仍须单独授权。
- 工作区启动时不干净则禁止自动提交和推送；保留已有修改，禁止 force push、`reset --hard`、`clean -f`。
- 长期状态只保存仍有效的指标基线、授权任务、未解决问题和决策；单次更新不超过 40 行。

## 输出

```markdown
## 自主进化评估
- 新增信号：无 / 简述
- 最高分候选：无 / 任务名（分数）
- 决策：实施 / 提案待授权 / 零修改结束
- 验证：无 / 结果
- 用户决策：无 / 具体事项
```
